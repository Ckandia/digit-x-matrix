services:
    - type: web
      name: digit-x-matrix-backend
      runtime: node
      rootDir: backend
      buildCommand: npm install
      startCommand: npm start
      envVars:
          - key: DERIV_APP_ID
            sync: false
          - key: ALLOWED_ORIGIN
            sync: false
          - key: DATABASE_URL
            sync: false
